// Truthy & falsy values
// Truthy and falsy values in JavaScript are values that evaluate to `true` or `false` in a boolean context, respectively.

// Falsy Values 
    // NaN, false, 0, null, undefined,""
    // these values returns false in the output 


// Truthy values
    // rest of the falsy values known as truthy values
    // these values returns True in the output 

let check = 0;              // replace any value with 0

if(check){
    console.log("Truthy Value")
}else{
    console.log("Falsy Value")
}


